Design assets for MatrixFace

Place the official PNG design exports here (public/design/) so the `MatrixFace` component can show them via `showDesign`.

Expected filenames (used in code):
- matrix-face-1.png  — static/base + talking frames grid
- matrix-face-2.png  — facial-expression tiled grid

Current placeholders: `matrix-face-1.svg`, `matrix-face-2.svg` (SVG placeholders included). Replace them with real PNGs or update component paths if you prefer SVG.
