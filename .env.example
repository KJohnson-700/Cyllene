# Copy to .env and fill in values

# Hermes API Server key (set in hermes config.yaml as platforms.api_server.key)
VITE_API_KEY=your_api_server_key_here

# Override API server base URL for production (leave empty for local dev proxy)
VITE_API_BASE=

# Override web server base URL for production
VITE_WEB_BASE=
